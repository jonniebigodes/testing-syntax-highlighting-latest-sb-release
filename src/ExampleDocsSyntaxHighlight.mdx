import { Story, Canvas, Meta } from "@storybook/blocks";

<Meta title="Getting Started" />

# Getting Started

To get started, you'll need to install the your package using one of the following methods:


## Bash examples

### Not working

This is a codeblock with the bash instructions with no language specified

```
// this is my broken codeblock
npm install ... //npm
yarn add ...     // yarn
pnpm add ...     // pnpm
```


### Working

#### Text

This is a codeblock with the bash instructions with the language specified as `text`

```text
npm install ... //npm
yarn add ...     // yarn
pnpm add ...     // pnpm
```

### Shell (Bash)

This is a codeblock with the bash instructions with a language specified as `bash`

```bash
npm install something
yarn add something      
pnpm add something
```

## Reducer example

> Note: The following reducer names are reserved :`chart`, `event`, `telemetry`

## Not working

This is a codeblock without any language specified

```
// You typically want to define these outside  of a React component or memoized them
// as if the component where to re-render, the customReducer would take a new reference
// value and cause the store to be re-initialized
const customReducers = {
    tooltips: (state={}, action) => {
        switch (action.type) {
            case "EVENT.MOUSE_ENTER":
              return { tooltip: true, ...state};
            default:
              return state;
        }
    }
}
```

### Working

#### Text

This is a codeblock with the language specified as `text`

```text
// You typically want to define these outside  of a React component or memoized them
// as if the component where to re-render, the customReducer would take a new reference
// value and cause the store to be re-initialized
const customReducers = {
    tooltips: (state={}, action) => {
        switch (action.type) {
            case "EVENT.MOUSE_ENTER":
              return { tooltip: true, ...state};
            default:
              return state;
        }
    }
}
```

#### JavaScript

This is a codeblock with the language specified as `js`

```js
// You typically want to define these outside  of a React component or memoized them
// as if the component where to re-render, the customReducer would take a new reference
// value and cause the store to be re-initialized
const customReducers = {
    tooltips: (state={}, action) => {
        switch (action.type) {
            case "EVENT.MOUSE_ENTER":
              return { tooltip: true, ...state};
            default:
              return state;
        }
    }
}
```


## Other examples

### JSON

This is an example with language specified as `json`

```json
{"name":"John", "age":30, "car":null}
```

### YML

This is an example with language specified as `yml`

```yml

# Workflow name
name: 'Chromatic Deployment'

# Event for the workflow
on: push

# List of jobs
jobs:
  test:
    # Operating System
    runs-on: ubuntu-latest
    # Job steps
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - run: yarn
        #👇 Adds Chromatic as a step in the workflow
      - uses: chromaui/action@v1
        # Options required for Chromatic's GitHub Action
        with:
          #👇 Chromatic projectToken, see https://storybook.js.org/tutorials/intro-to-storybook/react/en/deploy/ to obtain it
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
          token: ${{ secrets.GITHUB_TOKEN }}

```


### Markdown

This is an example with language specified as `markdown`

```md

# A Markdown Heading

## A Markdown Subheading

### A Markdown Subsubheading



> Lorem ipsum dolor sit amet, consectetur adipiscing elit. Donec a diam lectus. Sed sit amet ipsum mauris.
> Maecenas congue ligula ac quam viverra nec consectetur ante hendrerit. Donec et mollis dolor.

*italic*, **bold**, ~~strikethrough~~ and `monospace`

```


#### Markdown with Bash

````md

This is an example with language specified as `markdown` and a codeblock with language specified as `bash`


```bash
npm install something
yarn add something
pnpm add something
```
````

### CSS

This is an example with language specified as `css`

```css
.container {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  height: 100vh;
  width: 100vw;
  background-color: #f5f5f5;
}
```

### HTML

This is an example with language specified as `html`

```html
<div class="container">
  <h1>Heading</h1>
  <p>Paragraph</p>
</div>
```


### TSX

This is an example with language specified as `tsx`

```tsx
import type { Meta, StoryObj } from '@storybook/react';

import { Button, ButtonProps } from './Button';

const meta = {
  component: Button,
} satisfies Meta<typeof Button>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Primary: Story = {
  args: {
    primary: true,
    label: 'Button',
  },
};
```

### TS

This is an example with language specified as `ts`

```ts
import type { Meta, StoryObj } from '@storybook/react';

import { Button, ButtonProps } from './Button';

const meta = {
  component: Button,
} satisfies Meta<typeof Button>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Primary: Story = {
  args: {
    primary: true,
    label: 'Button',
  },
};
```


### GRAPHQL

This is an example with language specified as `graphql`

```graphql

query GetLaunches {
  launchesPast(limit: 10) {
    mission_name
    launch_date_local
    launch_site {
      site_name_long
    }
    links {
      article_link
      video_link
      mission_patch
    }
    rocket {
      rocket_name
    }
  }
}

```